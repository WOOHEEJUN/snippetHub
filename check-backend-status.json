{
    "DocumentName": "AWS-RunShellScript",
    "Parameters": {
        "commands": [
            "cd ~/app",
            "ps aux | grep java",
            "curl -s http://localhost:8080/api/health",
            "ls -la api-0.0.1-SNAPSHOT.jar"
        ]
    }
}
